kind = "web"
previewPath = "/"
title = "Orgni — Business context for AI"
version = "1.0.0"
id = "artifacts/frontend"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18130

[services.development]
run = "pnpm --filter @workspace/web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/web", "run", "build" ]
serve = "static"
publicDir = "apps/web/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18130"
BASE_PATH = "/"
