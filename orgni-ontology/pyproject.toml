[project]
name = "orgni-ontology"
version = "1.0.0"
description = "Orgni Organizational Ontology — Phase 1 (Invoice, Contract, Proof of Payment)"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.6",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
